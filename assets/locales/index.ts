const locales = {
    ...require('./login'),
    ...require('./home'),
    ...require('./groups'),
    ...require('./social'),
    ...require('./pendingActions'),
    ...require('./celebrations'),
};

export default locales;
